---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import SearchableCards from "../../components/SearchableCards.astro";

const entries = await getCollection("posts", ({ data }) => !data.draft);
const sorted = entries.sort((a, b) => +b.data.date - +a.data.date);

const dateFmt = new Intl.DateTimeFormat("es-ES", {
  year: "numeric",
  month: "short",
  day: "2-digit",
});

const items = sorted.map((entry) => ({
  type: "post" as const,
  title: entry.data.title,
  excerpt: entry.data.excerpt,
  date: dateFmt.format(entry.data.date),
  readTime: entry.data.readTime,
  tags: entry.data.tags,
  href: `/posts/${entry.id}`,
}));

const title = "Posts — Daniel Miralles";
const description =
  "Reflexiones cortas sobre arquitectura de software, sistemas distribuidos y lo que aprendí en producción. Lecturas de menos de 5 minutos, en español.";
---

<Layout title={title} description={description}>
  <section class="max-w-5xl mx-auto px-6 pt-16 pb-10">
    <p class="text-sm mb-4" style="font-family: var(--font-mono); color: var(--color-fg-muted);">
      <span style="color: var(--color-fg-dim);">$</span> cd ~/posts
    </p>
    <h1 class="text-4xl md:text-5xl font-bold mb-4 tracking-tight" style="color: var(--color-fg-primary);">
      <span style="font-family: var(--font-mono); color: var(--color-fg-dim); font-size: 0.55em;">## </span>Posts
    </h1>
    <p class="text-lg max-w-[60ch] leading-relaxed" style="color: var(--color-fg-muted);">
      {description}
    </p>
  </section>

  <section class="max-w-5xl mx-auto px-6 pb-24">
    <SearchableCards items={items} searchPlaceholder="buscar en posts…" />
  </section>
</Layout>
